# AGENTS.md — 所有 Agent 的起手规则

> ## PUBLIC REPOSITORY SECURITY BOUNDARY
> 本仓库是完全公开仓。任何 Agent 禁止提交 credential、生产服务器地址、SSH 配置、真实部署命令、服务器目录、数据库连接/备份信息、私有仓具体名称与内部结构。生产环境相关配置只能通过 gitignored local configuration 注入。

本项目由多个 Agent（Claude / Codex / DeepSeek 等）协作。**动手前先读这份文件。**

**角色是分配给任务的功能位，不是绑定给某个模型品牌的身份证。** 下表的角色名不等于"只能是某个模型"——人类直接指派时同样可以换人接手，照样按这个角色的可写路径与约束干活。唯一要求：**每次会话开工前声明这次你扮演哪个角色**，一次会话/一次 PR 只履行一个角色，不要一边写前端一边顺手改数据。

**2026-08-13 现状**：前端视觉与组件实现由 **Codex** 负责；**Claude** 承担架构/契约角色（Schema、多 Agent 任务分包），不再默认写前端展示层代码。详见下表。

**本文件与这套角色体系只覆盖这个公开仓库。**

## 项目是什么
女流66 的作品编年史网站：只索引、不搬运。收录 2010 年至今在优酷 / 哔哩哔哩 / 斗鱼 / 抖音 的视频与直播场次，做成带 hover 预览的时间轴。详见 [README.md](README.md)。

## 三条硬约束
1. **服务器上不存放任何视频文件**，只存元数据与外链。
2. **不编造数据**。日期、链接、标题、时长——没核实的一律留空或标 `confidence: low`，禁止用"合理推测"填充。
3. **Schema（`src/lib/schema.ts`）只由当前扮演"架构/契约"角色的 Agent 修改**。其他角色要加字段，先提交 schema 变更评审。

## 角色与可写路径
| 角色 | 常见执行者 | 可写 | 禁止 |
|---|---|---|---|
| 架构/契约 | Claude | `src/lib/schema.ts`、`AGENTS.md`、`CLAUDE.md` | 前端展示层代码（归"前端"角色） |
| 前端 | Codex | `src/app/**` `src/components/**` `src/lib/**`（除 `schema.ts`） | 批量写 `data/**`、改 Schema |
| 数据采集 | DeepSeek | `data/**` | `src/**` `scripts/**` 配置 |
| 工具/自动化 | Codex | `scripts/**` `.github/**` | UI 组件、Schema |
| 审校 | 独立模型，不自审 | 只提 review 与修订 PR | 新增未核实条目 |

## 提交前必须通过
```bash
npm run validate     # Schema 校验 + 唯一性 + 链接格式
```
数据 PR 不夹带代码改动，代码 PR 不夹带数据改动。一个 PR 只做一件事。

分支命名：`data/<批次>` · `feat/<功能>` · `chore/<工具>`

## 改动怎么才能上线

**合进 `main` ≠ 上线。** 公仓的 CI 只做校验，不构建、不部署，也没有任何定时任务。

1. 改动进 `main`（`npm run validate` 必须过）
2. **由人类在后台控制台确认部署**——控制台会提示发现更新
3. 部署完成后改动才出现在线上，耗时数分钟

所以交付时**不要**写"已上线"，只能写"已合进 main，待部署"。是否部署、何时部署由人类决定。

### 部署不会覆盖后台改过的文案

部署会把公仓当前内容作为**基线**导入后台内容库，导入是**加性**的：

- 公仓新增的条目 → 追加
- 后台已经改过的条目 → 一个字都不动，只更新它内部那份「公仓原值」的只读镜像

因此往 `data/**` 加数据、打标签，**不会**把管理员在后台写的文案打回旧版。看到「后台改的文案变回旧版」不要往这个方向查——见下一节。

## 前台文案的两层来源

排查「文案变回旧版」之前必读。前台每处文案有两层：

1. **构建期烤入值**——服务端渲染出来的那一层。构建时拉一次内容接口，把「那一刻的后台文案」写进静态 HTML（见 `src/lib/baked-content.ts`）。接口没烤到时才退到 `src/lib/narrative.ts` / `src/lib/site-copy.ts` 里的公仓基线
2. **后台当前值**——首屏之后由浏览器拉只读内容接口，按稳定 id 覆盖到第一层上

**接口拿不到时一律静默回退。** 这是有意设计（内容服务不可用不能让整站空白），但副作用是：请求失败不会报错也不会空白，而是表现为「后台改好的文案自己变回了旧版」。

排查第一步永远是**分清「后台数据不对」还是「前台没拿到」**：

```bash
curl -s https://i6i6.space/api/content/narrative | grep -o '你刚改的那句文案'
```

- **出不来** → 是后台数据或保存流程的问题。
- **出得来** → 后台数据是对的。往下看。

### 「出得来但前台还是旧的」怎么读

**先分清是「一直旧」还是「闪一下就好」，两者根因完全不同。**

- **刷新后短暂显示旧文案、随即自己变对** → 这是烤入值漂移，属于已知的固有行为，不是故障。静态 HTML 里的是**上次部署那一刻**的文案，浏览器拉到当前值后才覆盖上去。而部署检查只比对两个仓库的 git 提交，**改后台文案不产生提交，因此永远不会触发重新烤入**——放着不管漂移会一直增长。想消掉这段过渡只能重新部署一次，让烤入值追上来。

- **一直是旧的、刷新多少次都不变** → 才是真的没拿到。2026-08-25 起网关对内容接口加了兜底缓存：上游卡住或挂掉时它会继续发出最后一份成功取到的内容，而不是把失败透给前台。所以这种情况现在应该非常罕见，一旦出现，说明连兜底都没命中（例如缓存是冷的），属于需要服务器权限才能查的范畴。

⚠️ **不要再从「按 IP 限流」入手查。** 那是 2026-08-25 之前的排查路径，当时限流配额偏紧、每次页面加载又并发拉三份内容，确实会撞。现在配额已经放宽，且缓存挡在前面，限流基本不再是成因。

**这一步需要服务器访问权限，交给有权限的人，Agent 不要自己找路进去。**

⚠️ **稳定 id 是后台与前台之间唯一的对齐键。** 改 `narrative.ts` / `site-copy.ts` 里的文案可以，**改 id 会让后台的覆盖失配**——后台照样保存成功，前台却永远显示基线，且不报任何错。

## 需要人工批量核对一大批条目时

不要靠机器判读直接批量写 `data/**`——机器初判会错，错判一旦顺手确认掉就进了档案，
比不判更贵。有一个可复用的本地工具专门干这件事：`scripts/calibrate/`
（机器初判 → 人工逐条网页确认 → 编译成计划 → 走暂存/落盘）。用法见
[scripts/calibrate/README.md](scripts/calibrate/README.md)。

## 会话起手式
1. 声明你的角色与本次任务范围
2. 声明产出物（具体到文件路径）
3. 与人类确认后再动手

## 交付时必须一并说明
- 你**没能**确认的东西（这比你完成的部分更重要）
- 覆盖缺口
- 所有 `confidence: low` 条目及原因
