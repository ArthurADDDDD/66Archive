# AGENTS.md — 所有 Agent 的起手规则

> ## PUBLIC REPOSITORY SECURITY BOUNDARY
> 本仓库是完全公开仓。任何 Agent 禁止提交 credential、生产服务器地址、SSH 配置、真实部署命令、服务器目录、数据库连接/备份信息、私有仓具体名称与内部结构，以及后台内部表名、队列名或暂存实现细节。生产环境相关配置只能通过 gitignored local configuration 注入。

本项目由多个 Agent（Claude / Codex / DeepSeek 等）协作。**动手前先读这份文件。**

**角色是分配给任务的功能位，不是绑定给某个模型品牌的身份证。** 下表的角色名不等于“只能是某个模型”——人类直接指派时同样可以换人接手，照样按这个角色的可写路径与约束干活。唯一要求：**每次会话开工前声明这次你扮演哪个角色**，一次会话 / 一次提交批次只履行一个角色，不要一边写前端一边顺手改数据。

**2026-08-13 现状**：前端视觉与组件实现由 **Codex** 负责；**Claude** 承担架构/契约角色（Schema、多 Agent 任务分包），不再默认写前端展示层代码。详见下表。

**本文件与这套角色体系只覆盖这个公开仓库。**

## 项目是什么

女流66 的网络生涯档案：只索引、不搬运。收录 2010 年至今在优酷 / 哔哩哔哩 / 斗鱼 / 抖音等平台的视频与直播记录，并提供时间线、故事模式、Highlights、游戏 / 节目聚合、数据统计、画廊与协作校对。详见 [README.md](README.md)。

## 三条硬约束

1. **服务器上不存放任何视频文件**，只存元数据与外链。
2. **不编造数据**。日期、链接、标题、时长——没核实的一律留空或标 `confidence: low`，禁止用“合理推测”填充。
3. **Schema（`src/lib/schema.ts`）只由当前扮演“架构/契约”角色的 Agent 修改**。其他角色要加字段，先提交 schema 变更评审。

## 角色与可写路径

| 角色 | 常见执行者 | 可写 | 禁止 |
|---|---|---|---|
| 架构/契约 | Claude | `src/lib/schema.ts`、`AGENTS.md`、`CLAUDE.md` | 前端展示层代码（归“前端”角色） |
| 前端 | Codex | `src/app/**` `src/components/**` `src/lib/**`（除 `schema.ts`） | 批量写 `data/**`、改 Schema |
| 数据采集 | DeepSeek | `data/**` | `src/**` `scripts/**` 配置 |
| 工具/自动化 | Codex | `scripts/**` `.github/**` | UI 组件、Schema |
| 审校 | 独立模型，不自审 | 只提 review 与修订建议 | 新增未核实条目 |

## 提交前必须通过

```bash
npm run validate     # Schema 校验 + 唯一性 + 链接格式
```

数据变更不夹带代码改动，代码变更不夹带数据改动；无论是直接维护还是通过 PR，一次提交批次只做一件事。

使用分支 / PR 时，分支命名：`data/<批次>` · `feat/<功能>` · `chore/<工具>`。

## 改动怎么才能上线

**合进 `main` ≠ 上线。** 公仓的 CI 只做校验，不构建、不部署，也没有任何定时部署任务。

1. 改动进入 `main`（`npm run validate` 必须过）
2. **由人类在后台控制台确认部署**——控制台会提示发现更新
3. 部署完成后改动才出现在线上

所以交付时**不要**写“已上线”，只能写“已合进 main，待部署”。是否部署、何时部署由人类决定。

### 部署不会覆盖后台改过的文案

部署会把公仓当前内容作为**基线**导入后台内容库，导入是**加性**的：

- 公仓新增的条目 → 追加
- 后台已经改过的条目 → 一个字都不动，只更新它内部那份“公仓原值”的只读镜像

因此往 `data/**` 加数据、打标签，**不会**把管理员在后台写的文案打回旧版。看到“后台改的文案变回旧版”不要往这个方向查——见下一节。

## 前台文案的两层来源

排查“文案变回旧版”之前必读。前台每处文案有两层：

1. **构建期烤入值**——服务端渲染出来的那一层。构建时拉一次内容接口，把“那一刻的后台文案”写进静态 HTML（见 `src/lib/baked-content.ts`）。接口没烤到时才退到 `src/lib/narrative.ts` / `src/lib/site-copy.ts` 里的公仓基线
2. **后台当前值**——首屏之后由浏览器拉只读内容接口，按稳定 id 覆盖到第一层上

**接口拿不到时一律静默回退。** 这是有意设计（内容服务不可用不能让整站空白），但副作用是：请求失败不会报错也不会空白，而是表现为“后台改好的文案自己变回了旧版”。

排查第一步永远是**分清“后台数据不对”还是“前台没拿到”**：

```bash
curl -s https://i6i6.space/api/content/narrative | grep -o '你刚改的那句文案'
```

- **出不来** → 是后台数据或保存流程的问题。
- **出得来** → 后台数据是对的。往下看。

### “出得来但前台还是旧的”怎么读

**先分清是“一直旧”还是“闪一下就好”，两者根因完全不同。**

- **刷新后短暂显示旧文案、随即自己变对** → 这是烤入值漂移，属于已知的固有行为，不是故障。静态 HTML 里的是**上次部署那一刻**的文案，浏览器拉到当前值后才覆盖上去。而部署检查只比对仓库提交，**改后台文案不产生公仓提交，因此不会因为这件事自动重新烤入**。想消掉这段过渡只能重新部署一次，让烤入值追上来。

- **一直是旧的、刷新多少次都不变** → 才是真的没拿到。当前网关有内容兜底缓存，因此这种情况应该较少；若持续出现，需要由有生产权限的人检查内容服务与发布状态。

⚠️ **不要把生产侧排障细节写回这个公开仓库。** 需要服务器访问权限的步骤交给有权限的人处理，Agent 不要尝试补全或记录真实入口、命令、目录、地址。

⚠️ **稳定 id 是后台与前台之间唯一的对齐键。** 改 `narrative.ts` / `site-copy.ts` 里的文案可以，**改 id 会让后台覆盖失配**——后台照样保存成功，前台却可能一直显示基线，且不报任何错。

## 需要人工核对数据时

按任务形态选工具，不要混用，也不要让机器判读直接批量写 `data/**`：

- **单条、已经核验清楚的修正** → `npm run archive:edit -- <entryId>`，见 [`scripts/ARCHIVE-EDIT.md`](scripts/ARCHIVE-EDIT.md)。
- **一批条目补同一类字段 / 分类** → `scripts/calibrate/`：机器初判 → 人工逐条确认 → 编译计划 → 人工复核后走批准的写入链路。见 [`scripts/calibrate/README.md`](scripts/calibrate/README.md)。
- **全库健康清点 / 找矛盾与漏标** → `scripts/audit/`：把全部条目摆出来，机器只负责排序疑点，人类给最终判定。见 [`scripts/audit/README.md`](scripts/audit/README.md)。

`calibrate` 和 `audit` 的本地决策文件都不应自动改写正式档案；真正进入 `data/**` 前必须有清晰的人工确认与 evidence。

## 会话起手式

1. 声明你的角色与本次任务范围
2. 声明产出物（具体到文件路径）
3. 与人类确认后再动手

## 交付时必须一并说明

- 你**没能**确认的东西（这比你完成的部分更重要）
- 覆盖缺口
- 所有 `confidence: low` 条目及原因
