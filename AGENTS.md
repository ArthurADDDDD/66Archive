# AGENTS.md — 所有 Agent 的起手规则

> ## PUBLIC REPOSITORY SECURITY BOUNDARY
> 本仓库是完全公开仓。任何 Agent 禁止提交 credential、生产服务器地址、SSH 配置、真实部署命令、服务器目录、数据库连接/备份信息、私有仓具体名称与内部结构，以及后台内部表名、队列名或暂存实现细节。生产环境相关配置只能通过 gitignored local configuration 注入。

本项目当前使用 Codex、Claude 协作。**动手前先读这份文件。** 两者都可以独立完成用户交办的任务，不按模型品牌划分文件所有权。

**当前接手的 Agent 负责把任务推进到可交付状态。** 用户直接交办任务，即授权当前 Agent 在任务范围内检查和修改相关文件；无需用户另行指定角色或批准角色切换。一个任务可以涉及前端、契约、工具和文档，按实际需要完成相关改动，不扩展到无关工作；数据与代码仍须分批提交。

**2026-09-05 协作方式**：取消此前“Codex 专写前端、Claude 只做架构”的固定分工。额度不足、工具不可用或用户偏好导致换模型时，新接手的 Agent 可直接继续任务。不得仅因模型身份、工作类别或文件路径不同而停下、要求换模型，或把任务转交给另一个 Agent。

**2026-09-02 项目阶段**：站点主要产品功能已经基本收口，默认工作重心转为**补档、考据、来源复核和数据质量维护**。除修 bug、性能 / 兼容性 / 可访问性 / 安全问题，或人类明确提出的新需求外，Agent 不要因为“技术上能做”就主动扩页面、加字段或增加复杂交互。当前阶段说明见 [`docs/project-status.md`](docs/project-status.md)。

**本文件只覆盖这个公开仓库。**

## 公开信息与跨仓边界

- 本仓文件、注释、测试样例、提交信息、PR / issue、截图和日志附件都按完全公开内容处理。只写公开功能所需的信息；禁止夹带内部任务编号、私有文档引用、本机绝对路径或未脱敏输出。
- 公开站点域名与前台需要的公开 API 路径可以记录；生产 SSH 入口、主机地址、服务器目录、部署拓扑和内部实现不得因跨仓排障而写入本仓。公开访问地址不等于生产管理入口。
- 当前 Agent 可以完成用户任务所需的跨仓修改；进入目标仓先读该仓规则，各仓分别验证、分别提交。其他仓允许保存的信息，不因此获得公开授权。
- 私有资料只用于理解问题，落到本仓时改写为公开功能与契约说明，不复制内部记录。提交前检查本次完整 diff 和拟用提交 / PR 文案；拿不准的信息先省略，不妨碍完成不依赖它的工作。

## 规则与现状的依据

协作规则统一以本文件为准，项目优先级见 `docs/project-status.md`。历史任务、旧角色分工与迁移前记录用于追溯，不是当前授权或实时环境清单。技术现状检查当前代码、配置和实测结果；发现文档与实现冲突时说明差异，不凭旧记录重做已完成任务。

## 站点地址

**当前站点是 https://nvliu.wiki/ 。** 早先公布过的 `i6i6.space` 只保留一个跳转页，
不再承载站点本身——写文档、示例、链接一律用新地址。

代码里凡是需要站点地址的地方都走环境变量，不要新增写死的域名：

- `SITE_ORIGIN` —— 站点的公开地址，构建期烤入按它取数
- `CONTENT_BAKE_ORIGIN` —— 单次构建的覆盖值；`off` 表示完全不烤（只做校验、不发布产物的流水线用这个）

`src/lib/baked-content.ts` 里的 `FALLBACK_SITE_ORIGIN` 只是「谁都没配」时的最后兜底，
**不是切换开关**。换地址要改的是发布流水线传进来的值，改这个常量不会让发布构建改变行为。

## 项目是什么

女流66 的网络生涯档案：只索引、不搬运。收录 2010 年至今在优酷 / 哔哩哔哩 / 斗鱼 / 抖音等平台的视频与直播记录，并提供时间线、故事模式、Highlights、游戏 / 节目聚合、数据统计、画廊与协作校对。详见 [README.md](README.md)。

## 三条硬约束

1. **服务器上不存放任何视频文件**，只存元数据与外链。
2. **不编造数据**。日期、链接、标题、时长——没核实的一律留空或标 `confidence: low`，禁止用“合理推测”填充。
3. **Schema（`src/lib/schema.ts`）变更必须检查兼容性**。当前 Agent 可以完成必要的契约修改，但须检查现有数据、读写调用方及校验脚本，说明影响并完成相关验证。破坏性变更或含义不明确的数据迁移须先明确方案并取得用户确认；不因缺少另一个模型而阻塞兼容性修复。

## 按任务组织工作

以下是检查重点，不是模型权限表。Codex、Claude 均可按用户任务修改相关路径。

| 工作类别 | 常见路径 | 必须关注 |
|---|---|---|
| 架构/契约 | `src/lib/schema.ts`、`AGENTS.md`、`CLAUDE.md` | 兼容性、调用方、规则一致性 |
| 前端 | `src/app/**`、`src/components/**`、`src/lib/**` | 展示、交互、可访问性和回归验证 |
| 数据维护 | `data/**` | 证据、置信度、人工核对与批准的写入链路 |
| 工具/自动化 | `scripts/**`、`.github/**`、相关配置 | 可复现性、校验、安全边界 |
| 文档 | `README.md`、`docs/**`、相关说明文件 | 与当前实现一致，不泄露生产信息 |

- **默认由当前 Agent 完成实现与自检**。自检应如实标为自检，不声称是独立审校。用户明确要求独立审校时，再安排独立审校；无法完成时说明缺口，不把它变成所有任务的默认前置条件。
- **接续任务先读现状**：检查工作区改动、相关文件和已有交接记录，保留他人未提交的工作，继续尚未完成的部分。只有实际冲突或缺少必要信息时才问用户，不要求用户重复授权已明确的任务。
- **遇到真实阻塞要具体说明**：缺的是哪项权限、证据、工具或产品决定；先完成不受阻塞的部分。不得用“这应由 Claude/Codex 负责”代替处理问题。
- **规则集中维护在本文件**。`CLAUDE.md` 作为入口和简短接续提醒，不重复维护另一套角色边界。

## 提交前必须通过

```bash
npm run validate     # Schema 校验 + 唯一性 + 链接格式
```

数据变更不夹带代码改动，代码变更不夹带数据改动；无论是直接维护还是通过 PR，一次提交批次只做一件事。

使用分支 / PR 时，分支命名：`data/<批次>` · `feat/<功能>` · `chore/<工具>`。

## 改动怎么才能上线

**合进 `main` ≠ 上线。** 公仓的 CI 只做校验，不构建、不部署，也没有任何定时部署任务。

1. 改动进入 `main`（`npm run validate` 必须过）
2. **由人类在后台控制台确认部署**——控制台会提示发现更新
3. 部署完成后改动才出现在线上

所以交付时按实际状态说明“已本地修改”“已提交”或“已合进 main，待部署”，不得把尚未合并的改动说成已合并，也不要写“已上线”。是否部署、何时部署由人类决定。

### 部署不会覆盖后台改过的文案

部署会把公仓当前内容作为**基线**导入后台内容库，导入是**加性**的：

- 公仓新增的条目 → 追加
- 后台已经改过的条目 → 一个字都不动，只更新它内部那份“公仓原值”的只读镜像

因此往 `data/**` 加数据、打标签，**不会**把管理员在后台写的文案打回旧版。看到“后台改的文案变回旧版”不要往这个方向查——见下一节。

## 前台文案的两层来源

排查“文案变回旧版”之前必读。前台每处文案有两层：

1. **构建期烤入值**——服务端渲染出来的那一层。构建时拉一次内容接口，把“那一刻的后台文案”写进静态 HTML（见 `src/lib/baked-content.ts`）。接口没烤到时才退到 `src/lib/narrative.ts` / `src/lib/site-copy.ts` 里的公仓基线
2. **后台当前值**——首屏之后由浏览器拉只读内容接口，按稳定 id 覆盖到第一层上

**接口拿不到时一律静默回退。** 这是有意设计（内容服务不可用不能让整站空白），但副作用是：请求失败不会报错也不会空白，而是表现为“后台改好的文案自己变回了旧版”。

排查第一步永远是**分清“后台数据不对”还是“前台没拿到”**：

```bash
curl -s https://nvliu.wiki/api/content/narrative | grep -o '你刚改的那句文案'
```

- **出不来** → 先确认 HTTP 状态、响应格式和请求是否成功；请求成功但内容缺失时，再查后台数据或保存流程。单凭没有匹配到文字不能确定是后台数据错误。
- **出得来** → 后台数据是对的。往下看。

### “出得来但前台还是旧的”怎么读

**先分清是“一直旧”还是“闪一下就好”，两者根因完全不同。**

- **刷新后短暂显示旧文案、随即自己变对** → 这是烤入值漂移，属于已知的固有行为，不是故障。静态 HTML 里的是**上次部署那一刻**的文案，浏览器拉到当前值后才覆盖上去。而部署检查只比对仓库提交，**改后台文案不产生公仓提交，因此不会因为这件事自动重新烤入**。想消掉这段过渡只能重新部署一次，让烤入值追上来。

- **一直是旧的、刷新多少次都不变** → 才是真的没拿到。当前网关有内容兜底缓存，因此这种情况应该较少；若持续出现，需要由有生产权限的人检查内容服务与发布状态。

⚠️ **不要把生产侧排障细节写回这个公开仓库。** 需要服务器访问权限的步骤交给有权限的人处理，Agent 不要尝试补全或记录真实入口、命令、目录、地址。

⚠️ **稳定 id 是内容服务与前台之间唯一的对齐键。** **改 id 会让覆盖失配**——内容服务照样保存成功，前台却可能一直显示基线，且不报任何错。

⚠️ **改 `narrative.ts` / `site-copy.ts` 里已存在条目的文字，不会覆盖内容服务中该 id 的当前值。** 它仍可能影响接口不可用时的兜底显示。

这两个文件是**基线**，不是当前值。内容服务只在「这个 id 还不存在」时采纳基线；已经存在的
条目，基线改了也不会被采纳。于是改一条已存在的文案会得到这样一串结果：

> 本地看得见 → 推上去 → CI 通过 → 部署成功 → **线上一个字都没变**，全程零报错。

这个现象极易被误判成缓存或 CDN 问题，往那个方向查一整天也查不出来。**它不是 bug，
是设计成这样的**：这条规则保护的是维护者在内容服务里敲的字不被一次数据同步冲掉。

所以：

- **要改线上文案，去内容服务里改**，那是唯一会生效的地方。
- 确实需要从这个仓库这一侧改（比如批量整理措辞），改完要由维护者在内容服务侧把它
  同步过去——那边有一对显式的拉取 / 推送命令，两个方向都得人主动发起。
- **新增**一个此前不存在的 id 是例外：新 id 会在下一次数据同步时被采纳。

基线本身仍然要维护——它是接口不可用时前台的兜底值，离当前值越远，兜底那一刻显示的
东西就越错。

## 需要人工核对数据时

按任务形态选工具，不要混用，也不要让机器判读直接批量写 `data/**`：

- **单条、已经核验清楚的修正** → `npm run archive:edit -- <entryId>`，见 [`scripts/ARCHIVE-EDIT.md`](scripts/ARCHIVE-EDIT.md)。
- **一批条目补同一类字段 / 分类** → `scripts/calibrate/`：机器初判 → 人工逐条确认 → 编译计划 → 人工复核后走批准的写入链路。见 [`scripts/calibrate/README.md`](scripts/calibrate/README.md)。
- **全库健康清点 / 找矛盾与漏标** → `scripts/audit/`：把全部条目摆出来，机器只负责排序疑点，人类给最终判定。见 [`scripts/audit/README.md`](scripts/audit/README.md)。

`calibrate` 和 `audit` 的本地决策文件都不应自动改写正式档案；真正进入 `data/**` 前必须有清晰的人工确认与 evidence。

## 会话起手式

1. 简短说明本次任务范围与预计修改的文件；无需声明固定角色。
2. 用户已明确要求执行时，直接检查并动手，不再为常规、可逆的任务内修改重复请求确认。
3. 只有关键需求不明、破坏性操作、需要额外权限或本文明确要求人工确认的数据写入等情况才暂停对应步骤；继续推进不依赖该确认的工作。已有确认仍然有效，不因换模型而重问。

## 交付时必须一并说明

- 你**没能**确认的东西（这比你完成的部分更重要）
- 覆盖缺口
- 涉及数据变更时，列出本次涉及的所有 `confidence: low` 条目及原因；不涉及数据时注明不适用，无需重新清点全库
